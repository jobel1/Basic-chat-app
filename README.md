# Basic Chat App using Node.js, Express, and Socket.IO

Key Features:
- Real-time messaging using  Socket.IO
- Node.js & Express server to handle connections
- Basic UI with an input field and message list
